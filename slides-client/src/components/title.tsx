import React, { ReactElement, Fragment } from 'react'
import { Typography } from '@mui/material'

export default function Title(): ReactElement {
  return (
    <Fragment>
      <Typography variant="h4">Welcome to the Slides WebApp</Typography>
    </Fragment>
  )
}
